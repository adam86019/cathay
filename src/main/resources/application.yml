server:
  port: 8181
spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:mem:primarydb;MODE=Oracle
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect